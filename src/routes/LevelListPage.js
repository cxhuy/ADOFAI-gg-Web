import React from 'react';

// Stylesheets
import '../stylesheets/levelList.css';

const LevelListPage = () => {
    return (
        <div>
            a
        </div>
    );
};

export default LevelListPage;
